function calculateAreaOfRectangle(length, width) {
    return length * width;
}

// Example usage:
let length = 5;
let width = 10;
var area = calculateAreaOfRectangle(length, width);
console.log(area); 